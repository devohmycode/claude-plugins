{
  "description": "Tracker guard: during a triage, the working tree is read-only and nothing is written to GitHub; during a batch, denied paths and commands, protected branches and pushes are refused, and nothing is written to GitHub; commits are checked for forbidden trailers.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/guard.mjs\" pre",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/guard.mjs\" post",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
